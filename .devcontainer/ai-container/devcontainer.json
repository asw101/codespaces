{
  "name": "AI (Container)",
  "image": "ghcr.io/asw101/codespaces/ai:latest",
  // "build": {
  //   "dockerfile": "Dockerfile"
  // },
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.go",
        "GitHub.copilot",
        "GitHub.copilot-chat"
      ]
    }
  },
  "postCreateCommand": "bash -lc 'mkdir -p .vscode && if [ ! -f .vscode/mcp.json ]; then cp /opt/wassette/mcp.json .vscode/mcp.json; fi'",
  "postStartCommand": "bash -lc 'set -e; echo \u0027[ai-container] Versions:\u0027; node -v; npm -v; go version; az version --help >/dev/null 2>&1 && echo azure-cli installed || true; claude --version || true; gemini --version || true; wassette --version || true'"
}

